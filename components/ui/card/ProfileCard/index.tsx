const ProfileCard = () => {
  return <div>ReviewCard</div>;
};

export default ProfileCard;

/**
 * correct filter of skills layout: absolute positioning. and render only on search with listner
 * profile image update
 * correct sidebar profile image render
 * update profile states on booking, review, skill
 *
 *
 *
 *
 *
 *
 * <Low>api folder name change</Low>
 */
